# Patterns (Python regex, case-insensitive by default) for API-break lines that
# should NOT fail the gate. Each non-blank, non-comment line is a regex applied
# to a single break line as printed by swift-api-digester -diagnose-sdk
# (e.g. "KSCrashRecording(KSCrashMonitorType.h): Var MonitorType.experimental has been removed").
#
# Add a comment above each pattern explaining why the break is acceptable.

# Experimental APIs are not part of the public contract. Symbols whose path
# contains the word "experimental" can be removed/changed without notice.
\bexperimental\b
